using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testConflict : MonoBehaviour
{


    // Start is called before the first frame update
    void Start()
    {
        print("one pice best anime");
    }

    // Update is called once per frame

}
